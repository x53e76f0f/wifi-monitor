
# Wi-Fi Monitor

Мощный инструмент мониторинга Wi-Fi сетей для Linux систем, использующий команду `iw scan` для сбора детальной информации о беспроводных сетях.

## 📋 Возможности

* 🔍 **Сканирование Wi-Fi сетей** с полным набором параметров
* 📊 **Сбор данных** в CSV и JSON форматах
* ⏱️ **Мониторинг в реальном времени** с настраиваемым интервалом
* 📈 **Встроенная аналитика** собранных данных
* 🔐 **Определение типа безопасности** (WPA/WPA2/WPA3)
* 📡 **Поддержка стандартов** 802.11n/ac/ax (Wi-Fi 4/5/6)
* 💾 **Экспорт данных** для дальнейшего анализа

## 📦 Собираемые данные

Для каждой точки доступа собирается:

* **BSSID** - MAC-адрес точки доступа
* **SSID** - имя сети
* **Частота** - частота в МГц
* **Канал** - номер канала
* **Уровень сигнала** - в dBm
* **Безопасность** - WPA/WPA2/WPA3/OPEN
* **Стандарт Wi-Fi** - 802.11n/ac/ax
* **Beacon interval** - интервал маячков
* **Country code** - код страны (если транслируется)
* **Capability** - флаги возможностей
* **Last seen** - время с последнего обнаружения

## 🚀 Установка

### Требования

* **ОС:** Linux (Ubuntu, Debian, Fedora, Arch и др.)
* **Node.js:** >= 12.0.0
* **iw:** утилита для работы с беспроводными сетями
* **sudo:** права для выполнения `iw scan`

### Установка зависимостей

```bash
# Установка iw (если не установлен)
# Ubuntu/Debian
sudo apt-get install iw

# Fedora
sudo dnf install iw

# Arch Linux
sudo pacman -S iw

# Проверка установки
iw --version
```

### Установка проекта

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/wifi-monitor.git
cd wifi-monitor

# Установка зависимостей (если потребуются в будущем)
npm install
```

## 💻 Использование

### Командная строка

#### Базовый мониторинг

```bash
# Запуск мониторинга с настройками по умолчанию
node wifi-monitor.js

# Интерфейс wlp3s0, интервал 5 сек, формат: CSV + JSON
```

#### Настройка параметров

```bash
# Использование другого интерфейса
node wifi-monitor.js --interface wlan0

# Изменение интервала сканирования (в миллисекундах)
node wifi-monitor.js --interval 10000  # 10 секунд

# Выбор формата данных
node wifi-monitor.js --format csv      # Только CSV
node wifi-monitor.js --format json     # Только JSON
node wifi-monitor.js --format both     # CSV + JSON (по умолчанию)

# Изменение директории вывода
node wifi-monitor.js --output ./my_data

# Комбинация параметров
node wifi-monitor.js -i wlan0 -t 15000 -f csv -o ./scans
```

#### Одиночное сканирование

```bash
# Выполнить одно сканирование и выйти
node wifi-monitor.js --once

# Или через npm скрипт
npm run scan
```

#### Анализ данных

```bash
# Анализ собранных CSV данных
node wifi-monitor.js --analyze ./wifi_data/wifi_scan.csv

# Или через npm скрипт
npm run analyze
```

#### Справка

```bash
# Показать справку
node wifi-monitor.js --help
npm run help
```

### Использование как модуля

```javascript
const WiFiMonitor = require('./wifi-monitor');

// Создание монитора с настройками
const monitor = new WiFiMonitor({
  interface: 'wlp3s0',
  interval: 5000,
  outputDir: './wifi_data',
  format: 'both'
});

// Одиночное сканирование
async function singleScan() {
  try {
    const aps = await monitor.scanOnce();
    console.log(`Найдено ${aps.length} точек доступа`);
  
    // Работа с результатами
    aps.forEach(ap => {
      console.log(`${ap.ssid || '(hidden)'}: ${ap.signal_dbm} dBm на канале ${ap.channel}`);
    });
  } catch (error) {
    console.error('Ошибка:', error);
  }
}

// Непрерывный мониторинг
async function startMonitoring() {
  await monitor.startMonitoring();
}

// Анализ собранных данных
function analyzeData() {
  WiFiMonitor.analyzeCSV('./wifi_data/wifi_scan.csv');
}

// Запуск
singleScan();
// или
startMonitoring();
```

### Расширенные примеры использования

В файле `example.js` доступны готовые примеры:

```bash
# Запуск примеров (требуется sudo)
sudo node example.js 1  # Простое сканирование
sudo node example.js 2  # Поиск лучшей 5GHz сети
sudo node example.js 3  # Мониторинг конкретной сети
sudo node example.js 4  # Анализ интерференции каналов
sudo node example.js 5  # Поиск скрытых сетей
sudo node example.js 6  # Тепловая карта сигналов
sudo node example.js 7  # Кастомный экспорт данных
```

Примеры включают:

* 🔍 Фильтрацию по диапазону (2.4/5 GHz)
* 📊 Анализ загруженности каналов
* 🎯 Мониторинг конкретной сети
* 🔐 Поиск незащищенных сетей
* 📈 Статистику качества сигнала

## 📊 Форматы данных

### CSV формат

Файл: `wifi_data/wifi_scan.csv`

```csv
timestamp,interface,bssid,ssid,freq_mhz,channel,signal_dbm,last_seen_ms,capability,security,beacon_interval,country,ht_cap,vht_cap,he_cap
2025-12-14T10:30:45.123Z,wlp3s0,aa:bb:cc:dd:ee:ff,MyNetwork,2437,6,-45.0,100,ESS Privacy,WPA2,100,US,802.11n,802.11ac,
```

### JSON формат

Файл: `wifi_data/wifi_scan_2025-12-14T10-30-45-123Z.json`

```json
{
  "timestamp": "2025-12-14T10:30:45.123Z",
  "interface": "wlp3s0",
  "scan_count": 1,
  "total_aps": 15,
  "access_points": [
    {
      "timestamp": "2025-12-14T10:30:45.123Z",
      "interface": "wlp3s0",
      "bssid": "aa:bb:cc:dd:ee:ff",
      "ssid": "MyNetwork",
      "freq_mhz": 2437,
      "channel": 6,
      "signal_dbm": -45.0,
      "last_seen_ms": 100,
      "capability": "ESS Privacy",
      "security": "WPA2",
      "beacon_interval": 100,
      "country": "US",
      "ht_cap": "802.11n",
      "vht_cap": "802.11ac"
    }
  ]
}
```

## 📈 Анализ данных

После запуска анализа вы получите:

* **Топ-10 сетей** по среднему уровню сигнала
* **Интерференцию** - количество сетей на каждом канале
* **Распределение по безопасности** - WPA/WPA2/WPA3/OPEN
* **Статистику** - мин/макс/средний сигнал для каждой сети

Пример вывода:

```
╔════════════════════════════════════════╗
║         АНАЛИЗ ДАННЫХ Wi-Fi           ║
╚════════════════════════════════════════╝

Всего записей: 150
Уникальных сетей: 15

┌─────────────────────────────────────────┐
│  Топ-10 сетей по среднему сигналу       │
├─────────────────────────────────────────┤
│  1. MyHomeWiFi         -42.50 dBm (-45..-40) │
│  2. OfficeNetwork      -55.30 dBm (-58..-52) │
│  3. NeighborWiFi       -67.20 dBm (-72..-63) │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Интерференция (записей на канал)      │
├─────────────────────────────────────────┤
│ Канал  1:   25 ██████████                │
│ Канал  6:   45 ██████████████████████    │
│ Канал 11:   30 ████████████              │
│ Канал 36:   15 ██████                    │
└─────────────────────────────────────────┘
```

## 💡 Рекомендации по использованию

### Оптимальные интервалы сканирования

```bash
# ⚠️ НЕ рекомендуется (слишком часто - будут ошибки "busy")
sudo node wifi-monitor.js --interval 1000   # ❌ 1 секунда

# ✅ Рекомендуемые интервалы:
sudo node wifi-monitor.js --interval 5000   # ✓ 5 секунд (по умолчанию)
sudo node wifi-monitor.js --interval 10000  # ✓ 10 секунд (стабильно)
sudo node wifi-monitor.js --interval 30000  # ✓ 30 секунд (для долгого мониторинга)
```

### Минимизация ошибок "Device busy"

1. **Используйте интервал >= 5 секунд** - интерфейсу нужно время
2. **Временно отключите NetworkManager** для частых сканирований
3. **Используйте nmcli** как альтернативу (не требует root)

### Сбор данных без sudo (через nmcli)

Для мониторинга без root-прав можно использовать NetworkManager:

```bash
# Создайте альтернативный скрипт
cat > nm-scan.sh <<'EOF'
#!/bin/bash
while true; do
  timestamp=$(date -Iseconds)
  nmcli -t -f BSSID,SSID,FREQ,SIGNAL,SECURITY dev wifi list | \
    awk -F: -v ts="$timestamp" '{print ts","$0}' >> wifi_nmcli.csv
  echo "$(date): Сканирование выполнено"
  sleep 5
done
EOF

chmod +x nm-scan.sh
./nm-scan.sh
```

## 🔧 Примеры использования

### Поиск лучшего канала для роутера

```bash
# Собираем данные в течение часа
node wifi-monitor.js --interval 60000 &
sleep 3600
killall node

# Анализируем интерференцию
node wifi-monitor.js --analyze ./wifi_data/wifi_scan.csv
```

### Мониторинг качества подключения

```bash
# Отслеживаем свою сеть каждые 2 секунды
node wifi-monitor.js --interval 2000 --format csv

# В другом терминале следим за данными
tail -f ./wifi_data/wifi_scan.csv | grep "MyNetwork"
```

### Сравнение диапазонов 2.4 GHz vs 5 GHz

```bash
# После сбора данных
cat wifi_data/wifi_scan.csv | awk -F',' 'NR>1 {
  if ($5 < 3000) band="2.4GHz"; 
  else band="5GHz"; 
  print band, $7
}' | awk '{sum[$1]+=$2; count[$1]++} 
  END {for (b in sum) print b, sum[b]/count[b]}'
```

## 🛠️ Разработка

### Структура проекта

```
wifi-monitor/
├── wifi-monitor.js   # Основной модуль (550+ строк)
├── example.js        # Примеры использования
├── package.json      # Конфигурация NPM
├── .gitignore        # Git исключения
├── README.md         # Документация
└── wifi_data/        # Директория с данными (создается автоматически)
    ├── wifi_scan.csv                    # Накопительный CSV
    ├── wifi_scan_2025-12-14T10-30.json  # JSON снимки
    └── wifi_scan_2025-12-14T10-35.json
```

### Добавление новых функций

1. Форк репозитория
2. Создание feature ветки
3. Внесение изменений
4. Тестирование
5. Pull request

## 🐛 Решение проблем

### "Permission denied" при сканировании

```bash
# Убедитесь, что используете sudo
sudo node wifi-monitor.js

# Или настройте sudoers для iw без пароля (опционально)
echo "$USER ALL=(ALL) NOPASSWD: /usr/sbin/iw" | sudo tee /etc/sudoers.d/iw
sudo chmod 0440 /etc/sudoers.d/iw
```

### "Device or resource busy" - НАИБОЛЕЕ ЧАСТАЯ ПРОБЛЕМА

Эта ошибка возникает когда:

* NetworkManager уже использует интерфейс
* Сканирования происходят слишком часто
* Предыдущее сканирование не завершилось

**Решения:**

```bash
# 1. Увеличьте интервал сканирования (рекомендуется >= 5 секунд)
sudo node wifi-monitor.js --interval 5000

# 2. Для частых сканирований временно остановите NetworkManager
sudo systemctl stop NetworkManager
sudo node wifi-monitor.js --interval 2000
# После завершения запустите обратно
sudo systemctl start NetworkManager

# 3. Используйте nmcli вместо прямого iw (альтернативный подход)
# Создайте скрипт nm-wifi-monitor.sh:
#!/bin/bash
while true; do
  nmcli -t -f BSSID,SSID,FREQ,SIGNAL dev wifi list
  sleep 5
done
```

**Встроенная защита:**

Программа автоматически:

* ✅ Делает 3 попытки при ошибке "busy" с задержкой 1 сек
* ✅ Не падает при ошибках, а продолжает мониторинг
* ✅ Предупреждает о слишком коротком интервале
* ✅ Показывает статистику успешных/неудачных сканирований

### Не находит интерфейс

```bash
# Проверьте доступные интерфейсы
iw dev

# Вывод будет примерно таким:
# phy#0
#   Interface wlp3s0
#     ifindex 3
#     wdev 0x1
#     addr xx:xx:xx:xx:xx:xx
#     type managed

# Используйте правильное имя из вывода
sudo node wifi-monitor.js --interface wlp3s0
```

### Слишком много неудачных сканирований

Если видите сообщение о многих неудачных сканированиях:

```bash
# 1. Проверьте статус интерфейса
ip link show wlp3s0

# 2. Проверьте, что интерфейс up
sudo ip link set wlp3s0 up

# 3. Проверьте драйверы
dmesg | grep -i "wl\|wifi\|80211"

# 4. Перезапустите сетевой стек
sudo systemctl restart NetworkManager
```

### CSV файл пустой или неполный

```bash
# Проверьте права на директорию
ls -la wifi_data/

# Дайте права, если нужно
chmod 755 wifi_data/
```

### Программа зависает

```bash
# Убейте процесс
sudo pkill -f wifi-monitor

# Проверьте, что iw работает нормально
sudo timeout 5 iw dev wlp3s0 scan | head -20

# Если зависает - проблема в драйвере/железе
# Попробуйте перезагрузить модуль
sudo modprobe -r iwlwifi  # для Intel
sudo modprobe iwlwifi
```

## 📝 Лицензия

MIT License - свободно используйте и модифицируйте проект.

## 🤝 Вклад в проект

Приветствуются:

* Баг-репорты
* Запросы на новые функции
* Pull requests
* Улучшения документации

## 📧 Контакты

* **Issues:** [GitHub Issues](https://github.com/yourusername/wifi-monitor/issues)
* **Email:** your.email@example.com

## 🙏 Благодарности

* Команде разработчиков утилиты `iw`
* Сообществу Node.js
* Всем контрибьюторам

---

**Happy Monitoring! 📡**
